.DS_Store
.npm/
.vscode
.idea
.public
node_modules
npm-debug.log
coverage
.cache
.envrc
tsconfig.tsbuildinfo
.env
.npmrc

# TODO: remove once @livestock deps are published to a proper registry.
# Ignored for now because the lockfile resolves @livestock from the local
# Verdaccio / monorepo paths, which CI/CDP cannot reach.
package-lock.json
